Given /^I have an asset named "([^"]*)" which makes \$(\d+) per month$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Then /^"([^"]*)" income "([^"]*)" "([^"]*)" from now should be \$(\d+)$/ do |arg1, arg2, arg3, arg4|
  pending # express the regexp above with the code you wish you had
end